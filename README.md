# `@tritac/prettier-config`

Tritac Prettier configuration

## Usage

**Install**:

```bash
$ yarn add --dev @tritac/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@tritac/prettier-config"
}
```
